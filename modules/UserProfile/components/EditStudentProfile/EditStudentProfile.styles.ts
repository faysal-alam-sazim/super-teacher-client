import { createStyles } from "@mantine/core";

export const useEditStudentProfileStyles = createStyles((theme) => ({
  input: {
    label: {
      color: theme.colors.green[6],
    },
    marginBottom: 10,
  },
}));
